{
  "name": "zkp-airdrop-contracts",
  "version": "1.0.0",
  "description": "ZKP Privacy Airdrop Smart Contracts",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "lint": "solhint 'contracts/src/**/*.sol'",
    "lint:fix": "solhint 'contracts/src/**/*.sol' --fix",
    "clean": "hardhat clean",
    "deploy:testnet": "hardhat run scripts/deploy-testnet.ts --network optimism-sepolia",
    "deploy:mainnet": "hardhat run scripts/deploy-mainnet.ts --network optimism"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "^2.1.0",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@types/node": "^20.10.0",
    "hardhat": "^2.19.0",
    "solhint": "^4.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "circomlibjs": "^0.1.7",
    "dotenv": "^16.3.0"
  }
}
